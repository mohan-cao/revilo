package nz.co.revilo.Scheduling;

public class SchedulingAlgorithmManager extends AlgorithmManager {

    long upperBound = 0;

    public SchedulingAlgorithmManager(int processingCores) {
        super(processingCores);
    }

    protected void execute() {
        //priority queue about nodes to go to next
        //priority queue created from critical path method
        //priority queue includes branching on switching core and available tasks


        //analyse critical path

        //loop non-dependent tasks to create root nodes (place in core 1)

        //loop root nodes

        // loop through priority queue

        //get current optimal time
    }
}

// The pattern